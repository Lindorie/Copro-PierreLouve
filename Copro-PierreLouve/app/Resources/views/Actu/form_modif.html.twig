{% extends 'base.html.twig' %}

{% block body %}

<h2>{% block section_title "Modifier une actualité" %}</h2>

{# {{ form(form) }} #}

{{ form_start(form) }}
	<div class="form_content">
		{{ form_errors(form) }}
		{{ form_row(form.titre) }}
		{{ form_row(form.file) }}
		{{ form_row(form.texte) }}
		<input type="submit" value="Valider" />
	</div>
{{ form_end(form) }}

{% endblock %}


